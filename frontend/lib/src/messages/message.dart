import 'package:intl/intl.dart';

String drawerHeader() => Intl.message(
      "Pool Temperatur",
      name: "drawerHeader",
      desc: "Header for the drawer",
    );

String drawerStart() => Intl.message(
      "Start",
      name: "drawerStart",
      desc: "Start of the drawer",
    );

String drawerPoolSelect() => Intl.message(
      "Pool Wählen",
      name: "drawerPoolSelect",
      desc: "Pool select in the drawer",
    );

String title() => Intl.message(
      "Pool Temperatur",
      name: "title",
      desc: "Title of the app",
    );

String graphNoData() => Intl.message(
      "Keine Daten vorhanden",
      name: "graphNoData",
      desc: "No data for the graph",
    );

String humidity() => Intl.message(
      "Luftfeuchtigkeit",
      name: "humidity",
      desc: "Humidity",
    );

String outsideTemperature() => Intl.message(
      "Außen Temperatur",
      name: "outsideTemperature",
      desc: "Outside temperature",
    );

String poolTemperature() => Intl.message(
      "Pool Temperatur",
      name: "poolTemperature",
      desc: "Pool temperature",
    );

String pool() => Intl.message(
      "Pool",
      name: "pool",
      desc: "Pool",
    );

String outside() => Intl.message(
      "Außen",
      name: "outside",
      desc: "Outside",
    );

String error() => Intl.message(
      "Fehler",
      name: "error",
      desc: "Error",
    );
